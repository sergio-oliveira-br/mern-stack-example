# mern/compose.yaml

services:

  backend:
    build:
      context: .
      dockerfile: Dockerfile-backend
    ports:
      - "5050:5050"

  frontend:
    build:
      context: .
      dockerfile: Dockerfile-frontend
    ports:
      - "8080:80"
